# 宅建士 오답노트 사이트 — 요구사항 (Claude Code용)

## 0. 한 줄 요약
宅建士(일본 부동산거래사) 시험 과거문제를 연도별 PDF에서 파싱해 DB로 쌓고, **틀린 문제를 연도·분야·細目별로 필터해서 다시 풀고, 어디서 반복해서 틀리는지 보는** 개인용 오답노트 웹앱. 사용자는 1명(마나짱). 시험일은 **2026-10-18**이라 화려함보다 **오늘부터 바로 쓰는 것**이 최우선.

---

## 1. 이미 만들어져 있는 것 (프로젝트 폴더에 같이 넣어둠)

| 파일 | 내용 |
|---|---|
| `takken_db.json` | R7(令和7年) 50문제 전체 데이터. 아래 스키마. 24문제에 誤答 기록 있음 |
| `parse_takken.py` | 과거문제 PDF(텍스트형) → JSON 파서. `python3 parse_takken.py <pdf> R7 out.json` |
| `make_pdf.py` | DB → 오답노트 PDF (WeasyPrint, Noto Sans CJK JP). `--year --category --min-wrong` 필터 |
| `R7_question_answer.pdf` | 원본 문제 PDF 샘플 (마지막 페이지에 정답표) |

**이 두 스크립트는 그대로 재사용**하고, 웹앱은 그 위에 얹는 형태로.

### DB 스키마 (`takken_db.json`)
```json
{
  "meta": {"categories": ["権利関係","法令上の制限","税その他","宅建業法","免除科目"]},
  "questions": [
    {
      "year": "R7",
      "number": 2,
      "category": "権利関係",
      "subtopic": "保証・連帯保証",
      "stem": "問題文...",
      "subs": ["ア　...", "イ　..."],
      "choices": ["肢1", "肢2", "肢3", "肢4"],
      "answer": 3,
      "wrong_dates": ["2026-09-19"],
      "error_reason": ""
    }
  ]
}
```
- `category`는 문제번호로 자동 결정: 問1–14 権利関係 / 15–22 法令上の制限 / 23–25 税その他 / 26–45 宅建業法 / 46–50 免除科目
- `subtopic`(細目)는 문제문 보고 태깅 (区分所有法, 35条書面, 報酬 등). 파서는 빈칸으로 두고, 앱에서 편집 가능해야 함
- `wrong_dates`가 비어 있으면 정답. 길이 = 틀린 횟수
- `error_reason`은 자유 텍스트 + 선택 태그: `知識 / 実行エラー / 語彙 / 初見`

---

## 2. 필수 기능 (MVP — 이번 세션에서 끝낼 것)

### A. 문제 등록
1. 연도 PDF 업로드(또는 파일 경로 지정) → `parse_takken.py`로 파싱 → DB에 추가
2. 틀린 번호 입력(예: `1,2,3,7,14`) + 날짜 → 해당 문제 `wrong_dates`에 추가
3. 같은 연도를 다시 풀었을 때도 같은 UI로 틀린 번호만 입력하면 횟수 누적
4. 細目 미입력 문제는 목록에서 눈에 띄게 표시 → 인라인 편집

### B. 오답 목록 / 필터
- 필터: 연도(복수), 분야(복수), 細目, 誤答回数 ≥ N, error_reason 태그
- 예: 「전 연도 × 権利関係 × 2회 이상」, 「R7 × 業法」
- 목록에서 문제문 첫 줄 + 細目 + 誤答回数 표시

### C. 다시 풀기 모드
- 필터 결과를 1문제씩 카드로. 선택지 클릭 → 정답/오답 즉시 표시
- 틀리면 `wrong_dates`에 오늘 날짜 자동 추가, 맞으면 추가 없음
- 문제 하단에 `error_reason` 입력칸(태그 + 한 줄 메모)
- 세션 끝나면 결과 요약 (몇 문제 중 몇 개)

### D. 통계
- 표: 연도 × 분야 정답수 (예: R7 権利 9/14)
- 표: 細目별 누적 誤答回数 내림차순 ← **가장 중요한 화면**
- 표: error_reason 태그 비율 (知識 vs 実行エラー)

### E. PDF 출력
- 현재 필터 그대로 `make_pdf.py`에 넘겨 PDF 다운로드 버튼

---

## 3. 나중에 (MVP 이후, 시간 남으면)
- 문제 자체에 메모/해설 필드
- 細目 자동 태깅 (Claude API로 문제문 분류)
- 4点セット 체크리스트(又は/かつ, 数字, 語尾 등)를 다시 풀기 카드에 표시
- 모바일 레이아웃 다듬기

---

## 4. 기술 방향 (제안 — 더 단순한 게 있으면 그걸로)
- 단일 프로세스, 로컬 실행 우선. 예: **FastAPI + Jinja/HTMX** 또는 **Flask + vanilla JS**. React 등 빌드 파이프라인은 불필요
- 데이터는 `takken_db.json` 단일 파일 유지 (git으로 이력 관리). 문제 수가 12년치 = 600개라 DB 불필요. 나중에 SQLite로 옮겨도 되게 접근 함수를 한 모듈에 모아둘 것
- 폰트: 일본어 표시 확인 (Noto Sans CJK JP). PDF 쪽은 이미 WeasyPrint 사용 중
- 배포는 일단 `localhost`. 필요하면 나중에 VPS에 올림 (사용자가 VPS·PM2 운영 중이라 PM2로 올릴 가능성 있음 → 실행 명령을 하나로 정리해둘 것)
- 실행 방법과 새 연도 추가 절차를 `README.md`에

---

## 5. 작업 순서 제안
1. 기존 `takken_db.json` 읽어서 목록·통계 화면부터 (데이터 확인)
2. 다시 풀기 모드
3. 틀린 번호 입력 UI (기존 연도 재풀이)
4. PDF 업로드 → 파싱 → 신규 연도 추가
5. PDF 다운로드 버튼
6. README

각 단계 끝날 때마다 실행해서 보여주고 다음으로. 한 번에 다 만들고 마지막에 실행하지 말 것.

---

## 6. 주의
- 문제 텍스트는 파서가 뽑은 그대로 저장. 임의로 요약·수정하지 말 것
- 정답 번호는 PDF 정답표에서 가져온 것. 파싱 실패 시 `answer: null`로 두고 UI에서 경고
- 스캔본 PDF는 파싱 안 됨 → 그 경우 에러 메시지만 내고 중단 (OCR은 범위 밖)
- 사용자 UI 언어: 한국어 라벨 + 일본어 문제문 (분야명은 일본어 그대로)
